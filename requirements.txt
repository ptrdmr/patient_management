Django>=5.1
django-csp>=3.7
python-dateutil>=2.8.2
# Add these to requirements.txt if needed
django-debug-toolbar>=4.2  # For development debugging
pillow>=10.0.0  # If you handle image uploads
django-crispy-forms>=2.0  # For better form rendering